# Landing (esqueleto)

Placeholder de la landing pública. **No desarrollar todavía** — solo una página
"Próximamente". Cuando se retome:

```bash
cd landing
npm install
npm run dev   # http://localhost:3001
```
