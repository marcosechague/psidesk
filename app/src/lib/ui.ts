/** Título de página (h1). Único tamaño/peso para todas las pantallas: subir el
 *  peso/tamaño acá lo cambia en todas las que usan <PageHeader>. */
export const PAGE_TITLE = "text-3xl";

/** Título de sección/tarjeta fuera de <CardTitle> (mismo look que CardTitle). */
export const SECTION_TITLE = "font-heading text-lg leading-snug font-medium";

/** Sublabel de sección uniforme (mayúsculas tenues). */
export const SECTION_LABEL =
  "text-muted-foreground text-xs font-medium tracking-wide uppercase";
